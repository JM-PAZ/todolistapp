{% extends 'base.html.twig' %}

{% block title %}Actividades{% endblock %}

{% block body %}
    <h1>Actividades</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Descripción</th>
                <th>Categoria</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        {% for actividade in actividades %}
            <tr>
                <td>{{ actividade.id }}</td>
                <td>{{ actividade.name }}</td>
                <td>{{ actividade.descripcion }}</td>
                <td>{{ actividade.categoria.name }}</td>
                <td>
                    <a href="{{ path('actividades_show', {'id': actividade.id}) }}">Ver</a>
                    <a href="{{ path('actividades_edit', {'id': actividade.id}) }}">Editar</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">No se encuentran datos</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('actividades_new') }}">Crear</a>
{% endblock %}
